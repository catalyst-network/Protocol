trigger:
- develop

pool:
  vmImage: 'ubuntu-latest'

jobs:
- job: setupProtobuffs
  steps:
  - script: curl -OL https://github.com/google/protobuf/releases/download/v3.2.0/protoc-3.2.0-linux-x86_64.zip && unzip protoc-3.2.0-linux-x86_64.zip -d protoc3
    displayName: 'Get Protobuffs'
    condition: succeeded()

  - script: sudo mv protoc3/bin/* /usr/local/bin/ && sudo mv protoc3/include/* /usr/local/include/ && ls -la /usr/local/bin
    displayName: 'Intall Protobuffs'
    condition: succeeded()

  - script: sudo chown root /usr/local/bin/protoc && sudo chown -R root /usr/local/include/google
    displayName: 'Set permissions'
    condition: succeeded()

- job: sdkRust
  steps:
  - template: ./libs/sdk-rust/azure-pipeline.yml
  dependsOn: setupProtobuffs

- job: sdkJs
  steps:
  - template: ./libs/sdk-js/azure-pipeline.yml
  dependsOn: setupProtobuffs